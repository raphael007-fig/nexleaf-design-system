@PoltailDesign.md
@FIGMA-MAP.md

HARD RULE — NO EXCEPTIONS: never generate UI components from scratch. Every visible
element is composed from the Poltail design system (src/components via src/index.js;
`@ds` in prototype-hub). Tokens only, Polaris icons only, CSS for layout only. If no DS
component fits, propose a DS addition + Jira ticket instead of hand-rolling. Full rule:
.claude/skills/ds-components-only/SKILL.md
